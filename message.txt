feat: Update task update logic and implement task deletion

- Improved `updateTask` function in `taskController` to handle task assignments:
  - Update `TaskAssignment` if `assignedTo` is provided.
  - Save updated task with new assignment.

- Added `deleteTask` function in `taskController` to delete tasks and related assignments:
  - Delete task by `taskId`.
  - Delete related `TaskAssignment` entries.

- Added route for task deletion in `routes`

- Fixed bug preventing correct rendering of users assigned to projects.

- Implemented task deletion button in task table:
  - `handleDeleteTask` function calls `deleteTask`.
  - Updated task list state upon successful deletion.

- Added `deleteTask` API function in `api.js`