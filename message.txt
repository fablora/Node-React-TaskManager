feat: Refactor authentication, user model, project controller, and dashboard components

- **AuthController**:
  - Improved code structure by adding a reusable `assignToken` function.
  - Simplified `register` and `login` methods with new User model static methods.

- **User Model**:
  - Refactored schema to use `validator` package for email and password validation.
  - Added static methods `signup` and `login` for user registration and authentication.

- **ProjectController**:
  - Updated `getProjectsByUser` method to handle project retrieval by user ID.

- **Dashboard Component**:
  - Added JWT decoding to fetch user-specific projects and tasks.
  - Updated to allow project selection and display tasks related to the selected project.

- **API Service**:
  - Added detailed comments for all endpoint calls.